<script setup lang="ts">defineProps<{side:"left"|"right"; family:string[]; teacher:string[]}>();defineEmits<{finish:[]}>()</script>
<template><div class="advice-page"><p class="page-kicker">GROWING TOGETHER · 09</p><h2>{{side==='left'?'把发现带回家':'把发现带回课堂'}}</h2><p class="advice-intro">{{side==='left'?'在日常里留一点空间，让孩子自然地展示自己的方法。':'在课堂里提供多种路径，让每种思考方式都有机会被看见。'}}</p><div class="letter-notes"><article v-for="(item,index) in side==='left'?family:teacher" :key="item"><i>{{side==='left'?'⌂':'✎'}}</i><span>{{index+1}}</span><p>{{item}}</p></article></div><blockquote>继续给孩子机会去表达、尝试、修改和重新开始。</blockquote><button v-if="side==='right'" class="finished-badge" @click.stop="$emit('finish')">读完啦 ✦</button></div></template>
